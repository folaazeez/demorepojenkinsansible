---
  - hosts: all
    tasks:
    - name: install ftp server package
      apt:
        name: vsftpd
        state: installed
    - name: start ftp service
      service:
        name: vsftpd
        state: started
        enabled: yes
